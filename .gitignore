# Simulated data and pipeline outputs -- regenerated by R/simulate_toy_gwas.R,
# not checked in (keeps the repo tiny and every clone starts from a clean slate)
/data/
/results/

# Answer key -- do not publish where students can browse it before the workshop
/instructor/

# R / RStudio cruft
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# knitr/rmarkdown intermediates
*_cache/
*_files/
figure/
notebooks/*.html
notebooks/*.md

# OS cruft
.DS_Store

# Instructor-only answer key / build notes -- contain spoilers (causal SNPs,
# lambda_GC values, expected results). Kept in a private repo instead:
# github.com/malmarri/popgen-gwas-tutorial-solutions
/SOLUTIONS.md
/PROGRESS.md
